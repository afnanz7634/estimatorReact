import { render } from '@testing-library/react';
import React from 'react';
import { ZipCodeStores } from './zip-code-stores';

describe('ZipCodeStores', () => {
    it('should render successfully', () => {
        // TODO
        // const { baseElement } = render(
        //     <ZipCodeStores
        //         zipCode={'42564'}
        //         onSubmit={() => {}}
        //         onInputChange={() => {}}
        //         stores={[]}
        //         loading={false}
        //         showStores={true}
        //     />,
        // );
        //   expect(baseElement).toBeTruthy();
    });
});
